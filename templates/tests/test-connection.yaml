apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gpt-human-app.fullname" . }}-test-connection"
  labels:
    {{- include "gpt-human-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gpt-human-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
