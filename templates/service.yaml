apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpt-human-app.fullname" . }}
  labels:
    {{- include "gpt-human-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpt-human-app.selectorLabels" . | nindent 4 }}
